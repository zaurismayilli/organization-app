// import { all } from '@redux-saga/core/effects';
// import usersSaga from './usersSaga';

function* rootSaga() {
  //WHEN WE HAVE API THEN WE CAN USE THIS
  // all sagas run here
  // yield all([usersSaga()])
}

export default rootSaga;
